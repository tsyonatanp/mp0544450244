# Google Sheets API Configuration
# צור Service Account ב-Google Cloud Console וקבל את הפרטים הבאים:

GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Google Places API Configuration (לביקורות מגוגל)
# צור API key ב-Google Cloud Console עבור Places API:

GOOGLE_PLACES_API_KEY=your-google-places-api-key-here
GOOGLE_PLACE_ID=your-google-place-id-here
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key-here

# ⚠️ Google Places API Limits:
# - 100,000 requests per day (חינמי)
# - 10 requests per second
# - $17 per 1000 requests (אחרי החבילה החינמית)
# - Cache מופעל ל-24 שעות כדי להפחית שימוש
# - Rate limiting: 3 requests per hour per IP (הגבלה חזקה)
# - Global rate limiting: 2 requests per minute
# - רק 3 ביקורות מוחזרות בכל פעם

# Website Configuration
NEXT_PUBLIC_SITE_URL=https://marita-pinchasi-law.co.il
NEXT_PUBLIC_PHONE=054-4450244
NEXT_PUBLIC_WHATSAPP=972544450244

# Security Configuration
NODE_ENV=production
NEXT_PUBLIC_ENVIRONMENT=production

# הוראות להגדרת Google Sheets API:
# 1. היכנס ל-Google Cloud Console: https://console.cloud.google.com
# 2. צור פרויקט חדש או בחר קיים
# 3. הפעל את Google Sheets API
# 4. צור Service Account
# 5. הורד את קובץ ה-JSON
# 6. העתק את client_email ו-private_key לקובץ זה
# 7. שתף את ה-Google Sheets עם ה-Service Account

# ⚠️ אבטחה - הנחיות חשובות:
# 1. לעולם אל תעלה את קובץ .env.local לגיט
# 2. השתמש ב-API keys עם הגבלות מתאימות
# 3. הפעל HTTPS בפרודקשן
# 4. עדכן את ה-API keys באופן קבוע
# 5. השתמש ב-strong passwords לכל החשבונות
# 6. הפעל 2FA על כל החשבונות
# 7. עקוב אחר הלוגים לפעילות חשודה 